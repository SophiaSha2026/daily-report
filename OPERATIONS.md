# 运维手册

面向日常使用。改代码看 `CLAUDE.md`，接手项目看 `HANDOVER.md`。

---

## 一、日常你什么都不用做

每个交易日北京时间 **09:27:30** 收邮件（美东 21:27 夏令时 / 20:27 冬令时），
想看得更细就打开在线面板。除此之外没有任何需要你操作的步骤。

发信是**双阈值**，不是单个时刻：

| | 时刻 | 含义 |
|---|---|---|
| 软 | 09:27:30 | 到点才发，不会更早。Claude 提前跑完也要等到这一秒，所以正常每天都卡这一秒 |
| 硬 | 09:28:30 | Claude 慢时允许压过软时点，但绝不越过这条。真越线邮件顶部会写明，不会悄悄晚发 |

结构上保证不越线：Claude 起跑时刻被闸门钉在 09:25:50，硬超时 2 分钟，
最坏 09:27:50 被中断，加落盘和发信启动约 8 秒，也就 09:27:58，
离硬上限还有 32 秒。硬上限是报警器，不是常规路径。

四个定时任务已启用（GitHub Actions → 左侧 workflow 列表，状态都是 active）：

| 任务 | 北京时间 | 干什么 |
|---|---|---|
| `1-盘前候选池` | 每周一至五 08:23 | 全市场 5548 只 → 候选池，写 `cache/universe.parquet` 并提交 |
| `2-竞价选股` | **07:40** / 08:00 / 08:20 / 08:35 / 08:47 / 08:59 / 09:05 / 09:11 八个入口 | 采 T1-T4 竞价快照 → 打分 → Claude 写理由 → **09:27:30 发邮件** + 更新面板 |
| `3-刷新缓存` | 每周日 20:17 | 刷全市场代码表 |
| `4-刷新板块成分表` | 每周日 20:40 | 用真浏览器抓同花顺 90 个行业的成分 |
| `5-形态扫描` | 每周一至五 17:00 | 收盘后扫「启动-缩量回调-再启动」，发第二封邮件 + 第二个面板 |

外加一个**不在 GitHub 上**的备份触发器，见第五节。

### 每天两封邮件，两个面板

| | 时间 | 邮件标题 | 面板 |
|---|---|---|---|
| 竞价强弱榜 | 09:27:30 | `[竞价清单] ...` | https://sophiasha2026.github.io/daily-report/ |
| 形态榜 | 17:00 后 | `[形态清单] ...` | https://sophiasha2026.github.io/daily-report/pullback.html |

形态榜的条件（三段全部满足才上榜）：

1. **启动日**：涨停或涨幅 ≥5%，成交量 ≥ 前一日 1.5 倍，换手 5%~10%
2. **调整期**：启动日次日起 1~6 个交易日，缩量，最低价不破启动日最低价
3. **今天**：涨幅 ≥5%，成交量 ≥ 前一日 1.5 倍，换手 5%~10%

「量」指成交量（手），不是成交额。

**重要：大部分交易日是 0 只。** 2026-08-26 抽样实测过 30 个交易日，
其中 26 天全市场一只都没有，只有 3 天出票，平均每交易日约 1 只。
空榜也会发信并写明。**收到「0 只」是常态，不是流水线坏了。**

嫌太少想放宽，改 `config.yaml` 的 `pullback` 段，按实测归因收益从大到小：

```yaml
pullback:
  launch:
    vol_ratio_min: 1.3      # 原 1.5。落选里「启动日量比不足」占 51 例
    turnover_min: 4.0       # 原 5.0
    turnover_max: 12.0      # 原 10.0
  adjust:
    max_days: 10            # 原 6。落选里「窗口内无合格启动日」占 142 例，
                            # 把窗口拉长是最直接的解法
```

改完 push 就生效。想先看放宽后能多出多少票，不用等一天：
Actions -> `5-形态扫描` -> Run workflow -> 勾 `backtest`，几分钟出频率估计。

产物三个去处：

- **邮箱**：前 10 只，含竞价价、高开、量能、形态、评分、LLM 理由与风险，附
  三个同花顺分层板块 txt + 全候选池打分明细 csv
- **在线面板**：https://sophiasha2026.github.io/daily-report/
  点代码即复制，切到同花顺剪贴板识别框会自动弹出
- **仓库**：`data/YYYY-MM/auction_<日期>.parquet` 存全候选池当日快照，
  这是以后做阈值标定的自建历史库

---

## 二、五个入口只会发一封信

`concurrency: auction-daily` 让五个 cron 串行。第一个跑完会把
`data/.../auction_<日期>.parquet` 提交进仓库，后面排队的那个 checkout 时
看到该文件就整个 job 跳过。

竞价 job 还会自查候选池新鲜度：`cache/universe_meta.json` 里的日期不是今天，
就自己现建一份候选池（约 4 分钟）再往下走。所以**盘前那班 cron 被 GitHub
丢掉也不影响当天出单**。

---

## 三、板块表也是自动的

同花顺的成分接口挡裸 HTTP 客户端指纹——带正确的 v cookie 用 requests 请求
仍然 403，必须在真实浏览器会话里 fetch。所以 `4-刷新板块成分表` 单开一个
workflow，用 Playwright 起 chromium 在页面上下文里抓，每周日 20:40 自动跑。

抓不到时保留仓库里已提交的那张表，只发一条 warning，**workflow 仍然是绿的**。
这是刻意的：你不需要每周去看一眼有没有红叉。行业成分变动很慢，用上周的表
完全没问题。

当前这张表：90 个行业 / 5547 只，**全市场 5548 只里覆盖 5546 只**。
runner 上跑一轮约 7 分钟。

---

## 四、每天早上想确认跑没跑，看两处

**1. 收没收到邮件**（北京 09:27:30 / 美东 21:27 夏令时）
标题形如 `[竞价清单] 2026-08-24 · 10只 · 首位 神奇制药`。
收到「[竞价] 告警」说明当天没出单，正文写了原因。

**2. Actions 页面**：https://github.com/SophiaSha2026/daily-report/actions
`2-竞价选股` 当天那条是绿的就没事。

日志里值得看的两行：

```
T3: 1092/1092 只, 1.3s      ← 命中率低于 80% 说明被限流，把 fetch_quotes 的 workers 从 5 调到 3
特征 1092 只，初筛通过 29 只  ← 长期为 0 说明阈值太紧，去 config.yaml 放宽
```

---

## 五、本地为主，云端托底（2026-09-15 起）

三档优先级（用户 2026-09-15 定的）：

1. **手动**：控制台点按钮，开跑窗口内随时。
2. **本机自动**：到了「自动开跑时刻」还没手点，计划任务自己跑。计划任务从
   窗口一开就每 15~30 分钟敲一次，但自动时刻之前只拉远端、不开跑。
3. **云端**：本机根本没跑（没开机），GitHub cron + Cloudflare Worker 叫起
   云端兜底。早盘由云端代发；起涨预测云端算不了，只发提醒。

每条线的入口都是同一个：`src/local_run.py`（计划任务、控制台按钮都调它，
同一把锁，谁先起谁跑），跑完产物 commit + push。控制台「排期」页把这些规则
和「现在这一刻触发会怎样」显式列出来，以那里为准。

### 本机计划任务（定义在 `tools/setup_tasks.ps1`，改排期改它再重跑）

| 任务 | 美东触发 | 北京窗口（手动可点） | 自动开跑（北京） | 跑什么 |
|---|---|---|---|---|
| `DailyReport-Local-Morning` | 周日~周四 18:00 起每 15 分钟，共 3h15m | 06:00~09:16 | 08:30 | 早盘选股，09:27:30 发信 |
| `DailyReport-Local-Evening` | 周一~周五 04:30 起每 30 分钟，共 16h | 16:00~次日 08:30 | 16:30 | 起涨预测，目标日 = 最近一个已收盘交易日 |
| `DailyReport-Local-Learn` | 周一~周五 04:40 起每 30 分钟，共 16h | 16:00~次日 08:30 | 16:40 | 参数自学 |
| `DailyReport-Local-Sync` | 每天 00:15 起每 30 分钟 | 不限 | 不限 | 只拉远端：云端代跑的产物落到本地面板 |

窗口和自动时刻都在 `local_run.FLOWS`（北京时间）；美东触发时刻在
`tools/setup_tasks.ps1`。早盘的手动窗口是美东晚 18:00~20:30（冬令时
17:00~19:30），20:30 没点就自动跑；起涨预测的自动时刻 16:30 北京是美东凌晨，
实际上就是自动跑，机器没醒就等醒了补，到次日 08:30 北京都有效。

四个都带「登录时触发」、允许唤醒、允许电池、错过就补。反复触发是安全的：
`--if-needed` 跑过就不再跑、不在窗口只拉一次远端、正在跑就退出
（`state/lock/<flow>.json` 进程锁，控制台手点和计划任务不会各起一个）。

**晚间系统为什么能补到次日 08:30**：收盘后数据定死，目标日是「最近一个
已收盘的交易日」，北京 09-15 早上 07:00 补跑出的就是 09-14 的清单，
和 09-14 17:00 跑一模一样。机器整天没开、美东晚上才醒也赶得上下一个
交易日开盘。08:30 之后不跑，别抢竞价线 09:14 起的采样。

### 云端托底（`.github/workflows/`）

| workflow | 触发 | 做什么 |
|---|---|---|
| `auction.yml` 2-竞价选股 | cron 07:40/08:20/08:59/09:11 BJT + Cloudflare Worker 07:30 BJT 派发 | 照常采样；09:27:00 看到本地 claim 就等到 09:28:20 确认 sent：有 sent 只发布 Pages 面板，不发信、不提交数据；没 claim 或没 sent 就云端发信 |
| `evening_check.yml` 8-晚间托底检查 | cron 20:30 BJT + Worker 20:45 BJT | 起涨预测**云端算不了**（特征表 2.5GB 在本机，新浪源 runner 不通），只检查 origin/main 上有没有目标日的 out_breakout，没有就发一封「本机今天没跑」提醒。同一天只提醒一次（`state/alert/`） |

其余 workflow（形态扫描、盘中采样、自评估、刷新缓存、冒烟）全部只留
`workflow_dispatch` 手动入口，没有 cron。控制台总览的「云端托底」卡片读
origin/main 上的 workflow 文件：该开的两条没开、不该开的带了 cron，都会亮黄。

Cloudflare Worker 在 `tools/external-trigger/`，是第三层触发（GitHub cron
实测迟到甚至丢班，本机可能睡着）。改派发时刻改 `wrangler.toml` 和
`worker.js` 的 ROUTES，`npx wrangler deploy`。**09-12 本地化时忘了它**，
它每天 17:05 BJT 继续派发已经取消的形态扫描，09-14 还发了一封形态邮件，
09-15 才改掉。停一条线要把三层触发都停干净：GitHub cron、本机计划任务、
Worker。

### 让位协议

```
state/claim/<flow>_<date>.json   本地开跑时推送：「我来」
state/sent/<flow>_<date>.json    本地发信成功后推送：「我发了」
```

竞价线云端 09:25:50 还会看本地有没有推当天的数据快照（09:25:42 推），
推了就跳过云端的 Claude 分析，省订阅额度；之后本地要是没发出去，
云端 09:28:20 仍然兜底发信，只是没有文案（硬约束 2）。

### 踩过的坑：笔记本用电池时任务永远不跑

PowerShell 的 `New-ScheduledTaskSettingsSet` 默认
`DisallowStartIfOnBatteries = True`。拔了电源，任务就一直卡在 `Queued`
从不启动，而且 `LastTaskResult` 依然报 **0**，从任务计划界面上完全看不出
出了问题。`setup_tasks.ps1` 里四个任务都带
`-AllowStartIfOnBatteries -DontStopIfGoingOnBatteries`，别用默认设置。

自查：控制台「排期」页，或

```bash
schtasks /Query /TN DailyReport-Local-Evening /V /FO LIST
```

`Ready` 正常，`Queued` 就是被条件挡住了。日志在 `tools/local_flow_<flow>.log`（一线一个文件）。

### 还是没邮件怎么办

- 早盘：Actions → `2-竞价选股` → `Run workflow`，交易日北京 09:11 之前点都有效。
- 晚间：开机、登录，计划任务自动补跑；或控制台点「起涨预测」。云端补不了。
- 去 Actions 页面确认 workflow 还是 active（仓库 60 天无提交会被自动停用；
  流水线每天提交数据，正常不会触发这条）。

非交易日不用担心：各阶段都查新浪交易日历，非交易日直接退出，不发告警邮件。

---

## 六、想改参数

全部阈值在 `config.yaml`，改完 push 就生效，不用动代码。最常改的几个：

```yaml
output:
  top_n: 10              # 每天发几只
  min_score: 45          # 低于这个分不上榜
screen:
  gap_pct_min: 0.0       # 竞价涨幅下限，0 = 只要不低开就行
  gap_pct_max: 5.0       # 竞价涨幅上限，绝对 5%，20cm 的票也一样
  gap_pct_peak: 3.5      # 最理想的涨幅，打分在这里满分，往两边递减
  auc_ratio_min: 0.0104  # 量能下限，占昨日全天成交额，≈ 量比 2.5
  auc_ratio_max: 0.2080  # 量能上限，≈ 量比 50
  min_auc_amount_wan: 300  # 竞价成交额绝对下限，防小盘票比例失真
```

量比和 `auc_ratio` 的换算是 **量比 ≈ auc_ratio × 240**。想把量比下限改成 3 倍，
就写 `3 / 240 = 0.0125`。邮件和面板的「量能(量比)」列两个数都会显示。

改文案去 `prompts/analyst.md`，不要动 workflow 里的 YAML。

**注意**：所有阈值目前都是先验估计，没有实证支撑。`data/` 每天在攒真实快照，
攒够约 40 个交易日就可以拿自己的历史数据做标定。
