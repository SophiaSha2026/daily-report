# 爆发线实验日志

每一次尝试都记在这里，**包括失败的**。失败记录是防止将来重复踩坑的
唯一手段 —— 半年后想加某个特征时，这份日志能直接回答「试过了，没用」。

规则（来自设计文档 2.2 的分级响应）：每一级响应只测一次，禁止在验证集上
反复微调超参数直到达标。那不是改进，是把验证集变成第二个训练集。

协议：训练集 ..2025-02，验证集 2025-03..2025-12，holdout 2026-01..09 锁死。
所有数字都是**验证集**上的走向前结果，holdout 一次都没动过。

---

## 数据底座（2026-09-12）

```
5515 只  4,267,682 行  2023-01-16 .. 2026-09-11
主板 3189 / 创业 1398 / 科创 608 / 北交 320
训练表 4,263,516 行 × 87 特征
y_up 正样本率 3.428%（抽样阶段估的是 3.38%，吻合）
起涨点 y_t0 19,708 个    见顶 y_top 19,534 个
```

换手率和流通股本 100% 是真值（新浪源自带），没有一只走估算兜底。

---

## 实验 1：模型擂台（训练目标 y_t0）

| 模型 | 命中率 | 倍数 | 相对 L0 | 验收 |
|---|---|---|---|---|
| L0 逻辑回归（尺子） | 5.89% | 1.7× | — | ✗ |
| L1 LightGBM | 7.39% | 2.1× | +1.50pp | ✗ |
| L2 GRU | 5.89% | 1.7× | +0.00pp | ✗ |

**读法**：L1 比 L0 高 1.5 个百分点，说明瓶颈**不在**特征已经榨干，
模型还有空间。L2 序列模型没有带来任何提升 —— 这条要留意，如果后续
一直如此，说明 `_seq` 那个「从三个统计量线性重建 5 步序列」的近似
丢掉了太多信息，真要用序列模型得在 build 阶段把逐日序列单独落盘。

分月看有个现象值得记：**2025-12 那个月 L0 只有 1.0 倍**。那是个正样本率
5.0% 的大月（普涨），模型在普涨行情里没有选股优势。这和设计文档 2.1 的
预测一致：横截面百分位化能防止模型学成牛市探测器，但**防不了普涨时
选股本身就没意义**这件事。

---

## 实验 2：修正训练目标与评估目标的错配

**动机**：第一版设计训练用 `y_t0`（起涨第一天），评估用 `y_up`
（当天买入未来 20 日涨超 50%），理由是「y_t0 信号干净」。
擂台结果让我怀疑这个选择：起涨后第 2、3 天买入**同样**能涨 50%，
`y_up` 把它们算作正确答案，而 `y_t0` 把它们当负样本训练 ——
模型被教会了排斥本该选中的票。

| 训练目标 | 特征数 | 命中率 | 倍数 | 剔除极端月 | 零命中月 |
|---|---|---|---|---|---|
| y_t0（原设计） | 15 | 7.39% | 2.08× | 7.17% | 0 |
| **y_up（修正）** | **19** | **11.11%** | **3.13×** | **10.33%** | **0** |

**+3.72 个百分点，提升 50%。错配确实存在，原设计是错的。**

结论：**口径一致比信号干净重要得多。** 后续一律用 `y_up` 当训练目标。
`y_t0` 保留用于「一段行情算一次」的统计场景，不再当训练目标。

这一步属于分级响应的「修正逻辑错误」，不是调参 —— 训练目标和评估目标
必须一致是基本原则，不是可调的超参。

---

## 实验 3：用 y_up 重跑完整擂台

| 模型 | 命中率 | 倍数 | 相对 L0 |
|---|---|---|---|
| L0 逻辑回归（尺子） | 8.45% | 2.4× | — |
| **L1 LightGBM**（冠军） | **11.11%** | **3.1×** | **+2.66pp** |
| L2 GRU | 4.40% | 1.2× | **−4.06pp** |

**L2 序列模型在当前实现下是负贡献，比线性的尺子还差 4 个百分点。**

原因基本可以确定：`model.py` 的 `_seq` 是从 `__last/__mean/__slope`
三个统计量**线性重建** 5 步序列的，这个近似不但丢信息，还会引入
不存在的平滑结构。设计文档 5.3 说序列模型的价值在于保留次序，
但前提是喂给它**真的序列**。

要救 L2 只有一条路：在 build 阶段把逐日的 `[5 × 26]` 序列单独落盘
（训练表会膨胀 5 倍，约 12GB）。在主指标还没达标之前不值得做这个投入，
**L2 暂时退出擂台**。

## 实验 4：分级响应第 1 级 —— 加特征（放宽筛选阈值）

| IC 下限 | 特征数 | 命中率 | 倍数 | 剔除极端月 |
|---|---|---|---|---|
| 0.010（原） | 19 | 11.11% | 3.13× | 10.33% |
| **0.005** | **34** | **13.96%** | **3.93×** | **13.21%** |
| 0.002 | 40 | 13.29% | 3.74× | 12.28% |

**+2.85 个百分点。** 两端低中间高的 U 型 —— 0.002 反而下降，
说明 0.005 是真实最优点而不是碰巧选中。

读法：0.010 砍太狠，把「单个 IC 弱但组合起来有用」的特征丢了，
而 GBDT 恰恰擅长用这种组合；0.002 又放进了太多噪音。
已固化 `IC_MIN = 0.005`。

## 实验 5：清单长度（产品形态，不是调参）

| 每天几只 | 命中率 | 倍数 | 每月期望选中 |
|---|---|---|---|
| 5 | **16.04%** | 4.51× | 16.8 只 |
| 10 | 13.96% | 3.93× | 29.3 只 |
| 20 | 12.00% | 3.38× | 50.4 只 |
| 30 | 10.98% | 3.09× | 69.2 只 |

单调下降，符合预期：清单越短越精选。

**这个结果不能用来宣布达标。** 验收线 15% 是按 top 10 定的，
top 5 的 16.04% 是靠缩短清单换来的，拿它过关就是移动球门。
按原口径是 13.96%，差 1.04pp。

清单长度该定几只是**用户的产品决策**，这里只负责把取舍摆出来：
每天 5 只、每月约 17 只中有 16% 会涨超 50%；每天 20 只、每月约 50 只
中有 12% 会。

## 最终验收：holdout（2026-01..08）

> **【2026-09-16 标注：这一节的 14.43% / 4.96 倍作废，不许再引用。】**
>
> 两条独立的理由，任何一条单独成立都够：
>
> 1. **协议里有前视偏差。** 当时的走向前没做净化（训练集紧挨测试月的 20 天，
>    标签是用测试月最高价算的），筹码网格用整段序列的 min/max（格宽由未来
>    最高价决定）。实验 10 修完这两处，同一套数据在验证集上从 15.7% 掉到 12.6%。
> 2. **选票规则不是现在这一套。** 那次是「每天固定前 10、无 97 分门槛、
>    无板块系数、无风险剔除」。同一份逐月滚动打分实测：前 10 无门槛
>    11.16%（n=2070），生产口径 12.64%（n=783），板块最强/最弱从 1.63「通过」
>    变成 6.20「重度不通过」—— 一个假阴一个假阳。两条路现在统一走
>    `validate.pick`（`src/breakout/validate.py:102`），那是生产和验收唯一的一份实现。
>
> **封存数据已经用掉了，不能拿它重测。** 按新口径要一个诚实的验收数，
> 只能换一段**从没用过的**窗口 —— 最早是 **2026-10 起**（2026-09 之前的数据
> 分别落在训练集、验证集和这段已用掉的封存里）。在那之前，界面、邮件和
> 控制台一律报**验证集**数字（实验 10/11 那组），这一条 2026-09-13 已经执行
> （见本文件「顺带修掉的口径漂移」）。
>
> 下面的原文保留，作为「当时怎么想的」的存档。

**holdout 只动了这一次**，动之前模型已定稿（L1 LightGBM，IC_MIN=0.005，
34 个特征，训练目标 y_up，清单每天 10 只 —— top 10 是用户守的原口径，
没有改成 top 5 来凑线）。

| 模型 | 验证集段 2025-03..12 | **HOLDOUT 2026-01..08** | 倍数 | 零命中月 |
|---|---|---|---|---|
| L0 逻辑回归（尺子） | 8.89% | 12.75% | 4.38× | 0 |
| **L1 LightGBM**（定稿） | 13.96% | **14.43%** | **4.96×** | 0 |

holdout 月度：12% / 18% / 11% / 26% / 17% / 10% / 6% / 23%

### 最重要的一条：holdout 略高于验证集

13.96% -> 14.43%。**这是没有过拟合的直接证据。** 如果模型是在验证集上
搜出来的，holdout 会明显塌下去；实际它还高了 0.47pp，说明学到的是
可泛化的结构，不是验证集的噪音。

这条比命中率本身更值得看 —— 命中率高而 holdout 塌了的模型，上线就废；
命中率一般但两段一致的模型，实盘表现可预期。

### 验收表

| 指标 | 及格线 | holdout 实测 | 状态 |
|---|---|---|---|
| 清单 A 命中率（top10） | ≥ 15% | 14.43% | **差 0.57pp** |
| 零命中月份 | ≤ 1 个 | 0 个 | ✅ |
| 剔除极端月 | ≥ 10% | holdout 段无 2024-09/2025-12 | 不适用 |

**主指标差 0.57 个百分点，未达标。**

这里有个口径细节必须挑明，不能自己挑有利的那个说：

- 定 15% 这条线时，我写的理由是「4.4 倍基础比率」（当时按 3.38% 估算）
- holdout 段的实际基础比率是 **2.91%**，14.43% / 2.91% = **4.96 倍**
- 所以按**绝对百分比**口径差 0.57pp，按**倍数**口径超了 0.56 倍

两个口径结论相反。选哪个是用户的决定，不是我的 —— 我事后挑一个有利的
口径宣布通过，就是移动球门。

## 特征重要性（定稿模型）

| 特征组 | 占比 | 读法 |
|---|---|---|
| 市场环境 | **27.3%** | 最高。模型有相当一部分在做**择时**而非选股 |
| **股东人数** | **17.0%** | 用户指定的特征，`gdhs_chg1__slope` 单项排第 3 |
| 位置 | 15.6% | |
| **筹码**（自算） | 11.6% | 自己算筹码这个决定有回报 |
| 换手 | 7.3% | 精确换手率（新浪源）值这个投入 |
| 均线 | 6.6% | 只留 ma_align 和斜率是对的，四个乖离丢掉没损失 |
| 压缩 / 波动 / 量能 / 相对强度 | 13.7% | |
| **交互项** | **0.7%** | **几乎无用** |

两条要记住的：

**一、市场环境占 27.3% 值得警惕。** 横截面百分位化保证了个股特征里
不含大盘信息，市场环境是**故意**单独给的坐标（见设计 4.1 的 Q5）。
但它排到第一，说明模型很大一部分能力来自判断「今天是不是好日子」。
好消息是 holdout 上仍然稳（两段一致），说明这个择时能力是真的；
坏消息是平盘市里清单的价值会明显下降 —— holdout 里 2026-07 只有 6%
就是这个现象。

**二、三个交互项加起来 0.7%，等于白做。** 我在设计文档 4.2 里论证得
很充分（「户数减少且筹码集中 = 两个独立证据同向」），实际 GBDT 自己
就能学到这种组合，手工交互项是冗余的。
**这条留给将来：给树模型手工造交互项，先问问它是不是已经会了。**

## 历史进度（已完成的实验）
### 各阶段验收进度对照

设计文档 2.2 定的验收表（在 **holdout** 上验收，下面是验证集的进度参考）：

| 指标 | 及格线 | 当前（top10, IC0.005） | 状态 |
|---|---|---|---|
| 清单 A 命中率 | ≥ 15% | 13.96% | 差 1.04pp |
| 剔除 2024-09 / 2025-12 后 | ≥ 10% | 13.21% | ✅ |
| 零命中月份 | ≤ 1 个 | 0 个 | ✅ |

三条里过了两条，主指标差 1.04 个百分点。

**到此为止已经用掉了分级响应的第 1 级（加特征）和第 2 级（换模型）。**
再往下只剩第 3 级（改事件定义）。按规矩，不许在验证集上继续微调 ——
那会把验证集变成第二个训练集，而 holdout 只能用一次，用坏了就没有
诚实估计了。

所以下一步是**产品决策**，不是技术决策：清单定几只。
top 5 达标（16.04%），top 10 差一点（13.96%）。


---

## 实验 6：分数和名次到底代表什么（2026-09-13）

用户问了两个问题，都用数据答，不靠直觉。

### 分数越高越准吗 —— 是，严格单调

验证集 10 个月逐月滚动，记录全市场每只票的分数和结果，111 万个样本：

| 分数段 | 样本数 | 涨超 50% | 相对随便买 |
|---|---|---|---|
| 0~49 | 647,271 | 2.11% | 0.72× |
| 50~69 | 202,282 | 3.31% | 1.13× |
| 70~79 | 100,307 | 3.78% | 1.29× |
| 80~89 | 109,721 | 4.49% | 1.53× |
| 90~94 | 43,623 | 6.18% | 2.11× |
| 95~98 | 10,859 | 8.08% | 2.76× |
| 99~100 | **213** | 11.74% | 4.00× |

没有一档倒挂。两个注意：99 分那档只有 213 个样本，数字不稳；
0~49 分（2.11%）比全市场基准（2.93%）还低，低分是**负**信号不是无信号。

### 第 1 名最准吗 —— 是，且统计显著

| 名次 | 命中 | ±标准误 |
|---|---|---|
| 第 1 | **25.12%** | 3.01% |
| 第 2 | 21.26% | 2.84% |
| 第 10 | 9.66% | 2.05% |
| 第 20 | 6.76% | 1.75% |

第 1 名 vs 第 10 名差 15.46 个百分点，合并标准误 3.65 —— 超过 4 个标准误。
中间名次有抖动（第 3 名低于第 4 名），每档只有 207 个样本，属正常噪音。

### 排序用整数分数是个 bug

两次实验的「第 1 名」对不上：25.12% vs 21.74%。原因是 `daily.py` 按
**取整后的分数**排序，而前几名的分数取整后大量并列（都是 97~99），
排第 1 的往往不是真正预测值最高的那只。**白丢 3.4 个百分点。**

已修：排序一律用连续预测值，整数分数只用于显示。
凡是「算出连续值 -> 取整展示 -> 拿展示值排序」的地方都有这个坑。

### 要精确度的话该怎么配

| 规则 | 精确度 | 倍数 | 每天 | 空仓天 | 每月命中 |
|---|---|---|---|---|---|
| 前 10 名（当前） | 14.35% | 4.04× | 10 | 0 | 30.1 |
| 前 5 名 | 16.91% | 4.76× | 5 | 0 | 17.8 |
| 前 3 名 + ≥97 分 | 22.17% | 6.24× | 2.0 | 37 | 9.1 |
| 前 2 名 + ≥97 分 | 25.00% | 7.03× | 1.4 | 37 | 7.6 |
| 前 1 名 | 25.12% | 7.07× | 1.0 | 0 | 5.3 |
| 前 1 名 + ≥97 分 | 25.29% | 7.12× | 0.8 | 37 | 4.4 |

**分数门槛 90 分完全没用**（和不设门槛结果一模一样），因为前 10 名本来
就都在 90 分以上。真正起作用的是砍短清单。

**精确度到 25% 附近就撞墙**：从前 2 名收到前 1 名，精确度只多 0.12 个
百分点，每月命中却从 7.6 掉到 5.3。再收紧是纯亏。

规则**暂未修改**，仍是前 10 名，等用户决定。


---

## 实验 7：最大化准确率（2026-09-13）

用户重申最初指令并强调「我要的是最大化准确率」。重读原文发现**两条一直
没实现的要求**：

  «如果当天没有符合要求的个股，清单可以为空»
      -> 清单该是**门槛制**（够格才上），不是「每天固定取前 N 名」。
         之前一直用 top-10，弱势日也硬凑满，纯粹拉低准确率。

  «如某个个股持续符合条件，可以连续几个交易日推荐»
      -> **连续够格的天数**，这一条完全没实现，而它是最强的单一信号。

### 实测（验证集 207 个交易日，全市场基准 3.55%）

| 规则 | 准确率 | 倍数 | 样本 | 每天 | 空仓天 |
|---|---|---|---|---|---|
| 前 10 名（旧规则） | 14.35% | 4.04× | 2070 | 10 | 0 |
| ≥97 分（只有门槛） | 15.43% | 4.34× | 1141 | 5.5 | 37 |
| ≥97 分 **连续 2 天** | 25.81% | 7.26× | 248 | 1.2 | 105 |
| ≥97 分 **连续 3 天** | **32.04%** | **9.01×** | 103 | 0.5 | 144 |
| ≥98 分 前 1 名 | 27.83% | 7.83× | 115 | 0.6 | 92 |
| 前 1 名（不设门槛） | 25.12% | 7.07× | 207 | 1.0 | 0 |

**两条结论**：

1. **光有分数门槛没用。** ≥97 分不看连续只有 15.43%，和旧的前 10 名
   （14.35%）几乎一样。≥90 分的门槛更是完全无效 —— 前 10 名本来就都在
   90 分以上，加不加这个条件结果一模一样。
2. **门槛 + 持续性才有效。** 15.43% -> 25.81% -> 32.04%，单调且幅度很大。

### 采用的规则

    上榜条件：分数 ≥ 97，够不到就空着（CAP_A=10 只是防极端强势日的上限）
    排序：先按连续够格天数，再按预测值
    准确率按连续天数分档报，不报一个笼统平均值

### 顺带修掉的 bug：排序用了取整后的分数

`daily.py` 先把预测值映射成 0~100 整数分数，再**按整数排序**。前几名
取整后大量并列（都是 97~99），排第 1 的往往不是真正预测值最高的那只。
两次实验的「第 1 名」命中率对不上（25.12% vs 21.74%）才发现，
**白丢 3.4 个百分点**。已改成排序用连续预测值，整数只用于显示。

凡是「算连续值 -> 取整展示 -> 拿展示值排序」的地方都有这个坑。

### 五天实跑（2026-09-07 ~ 09-11）

按新规则连跑 5 个交易日，连续天数正常累积：

    09-07  10 只，全是首日
    09-08  10 只，6 只连续 2 天
    09-09  10 只，3 只连续 3 天（英集芯/迅捷兴/扬子新材）
    09-10  10 只，2 只连续 4 天
    09-11  10 只，2 只连续 5 天（迅捷兴 688655、英集芯 688209）

这 5 天的结果已落盘 `data/breakout/2026-09/`，作为正式发信的起点：
2026-09-14（北京周一 16:30）起每天自动发，连续计数接得上，
迅捷兴和英集芯若继续够格就是第 6 天。

每天都撞上限 10 只，说明够格的其实更多。要再提准确率，两个旋钮：
门槛 97 -> 98，或上限 10 -> 5。**未调，等用户决定。**

## 实验 8：清单 A 改看「过去 5 个交易日」能到多少（2026-09-13）

用户问：A 清单改成每天根据过去 5 个交易日（含当天）的数据来出，
准确率会到多少。

脚本 `src/breakout/exp_window.py`，口径和实验 7 完全一致（验证集
2025-03 ~ 2025-12 共 207 个交易日，逐月走向前重训，y_up = 未来 20 个
交易日最高价涨超 50%，全市场基准 3.55%）。打分结果缓存在
`data/breakout/raw/wf_scores.parquet`，换口径不用重训。

### 先纠一个前提

「用过去 5 天的数据」如果不加次数要求，选出来的是同一批票：当天够格的
必然在 5 日窗口内至少出现 1 次。有意义的是**窗口内够格几次**。

### 实测（基线 = 现在的清单 A：≥97 分，当天按预测值取前 10）

| 规则 | 准确率 | 倍数 | 样本 | 每天几只 | 空仓天 |
|---|---|---|---|---|---|
| 现规则，上榜即列 | 18.04% ±1.35 | 5.07x | 815 | 3.94 | 37 |
| + 近 5 日内够格 ≥2 次 | 24.92% ±2.40 | 7.01x | 325 | 1.57 | 95 |
| + 近 5 日内够格 ≥3 次 | 28.85% ±3.63 | 8.11x | 156 | 0.75 | 128 |
| + 近 5 日内够格 ≥4 次 | 30.14% ±5.37 | 8.48x | 73 | 0.35 | 159 |
| + 近 5 日内够格 ≥5 次 | 35.00% ±10.67 | 9.85x | 20 | 0.10 | 190 |

同一次运行的连续版对照：连续 2 天 25.65%、3 天 31.31%、4 天 32.61%、
5 天 35.00%（连续 5 天和窗口 5 次是同一批票）。

「5 日平均预测值取前 1 名」只有 20.69%，明显不如门槛 + 持续性，
这条路排除。

### 结论

1. 同样要求 k 次，窗口版比连续版低 1~2.5 个百分点，都在误差范围内
   （n=99~156 时 ±3.6~4.7），说不上谁真更好。
2. 窗口版的真实好处不是准确率，是**抗断档**：停牌、节假日缺数据、
   某天差一分掉出去，连续计数归零，窗口计数不归零。代价是出票量，
   ≥3 次每天 0.75 只 vs 连续 3 天 0.48 只。
3. **用户决定不改入选规则**，正式清单继续只看当天，连续天数只用来
   排序和展示。

### 顺带修掉的口径漂移

实验 7 那组数字（32.0 / 25.8 / 15.4）是在「每天只看前 30 名」的口径下
算的，而生产是「≥97 分按预测值取前 10」。邮件、面板、控制台成绩条一直
挂着前一组，等于**拿另一个口径的成绩给现在的清单背书**。实验 8 用生产
口径重算并替换：

    首日/整体   15.4% -> 18.0%
    连续 2 天    25.8% -> 25.7%
    连续 3 天    32.0% -> 31.3%
    新增 4 天 32.6%、5 天 35.0%（样本 46 和 20，误差大，界面上标了样本数）

控制台的晚间成绩条原来挂的是 14.43%，那是**旧规则**（每天固定 10 只、
不设门槛）在封存数据上的验收成绩，同样是给新规则背书，已一并换掉。
封存数据只许看一次，已经用掉了，所以现在界面上报的一律是验证集数字。

### 清单 B 补齐列（用户要求）

邮件和面板的清单 B 原来只有 代码 / 名称 / 曾用最高分 / 距高点，
现在和清单 A 对齐：代码 / 名称 / 分数 / 上榜天数 / 历史准确率 / 现价，
末尾保留 进榜后涨幅 / 距高点。

为此 `state/breakout/a_pool.json` 每只票多记两个数：`days`（一共上过
几天清单 A）和 `streak`（最长连续天数）。同一天重复跑不会把 days 加两次。
已有的池按 `data/breakout/2026-09/` 那五天重放补齐。

清单 B 那列准确率是它**当初在清单 A 上**那一档的成绩，不是「见顶判断有
多准」：清单 B 现在是规则判定，还没有实测成绩。这句话印在表头下面。

`selftest_breakout.py` 加了 `check_table_shape`：表头数、数据行列数、
空表 colspan 三者必须相等。加一列忘了改表头只会让整行错位一格，
浏览器不报错，是静默故障。

---

## 实验 9：加成交量组和股东人数密度，顺带发现股东人数在偷看（2026-09-15）

用户要求清单 A/B 的参考特征加上**成交量**和**股东人数**。先核对现状：

- 成交量：只有 `vol_compress`（5 日/20 日均量）和 `vol_ratio5`（今天/5 日均量）
  两个「相对自己」的比值，没有量的方向和水平。
- 股东人数：四个变化量特征都在（`gdhs_chg1/chg3/down_streak/stale_days`），
  但对齐方式有问题，见下。
- 用户以为在里面的**成交额**和**资金流入**：都不在。东财资金流只有 120 天，
  够不着三年训练集。

### 先修两个 bug

**1. 股东人数按「报告期 + 15 天」对齐是偷看。** 东财的表自带公告日期列，
68208 条里 99.7% 的公告晚于报告期 + 15 天：中位滞后 50 天，九成分位 115 天
（年报里的户数次年四月底才公开）。也就是说模型平均提前一个多月「知道」了
户数变化，而这一组特征重要性排第二、`gdhs_chg1__slope` 单项排第三。
现在按公告日期对齐，缺公告日的按报告期 + 120 天兜底。selftest 用一条假公告
钉住「公告前看不到、公告当天起可用」。历史教训 25。

**2. `gdhs_stale_days` 一直是 0。** merge_asof 之后拿左表日期减自己，永远 0，
常数特征在筛选那一步被静默丢掉，「数据有多旧」从没进过模型。已修。

### 加的特征（102 列，原 87）

| 组 | 特征 | 定义 |
|---|---|---|
| 成交量（新组） | `vol_ratio20` | 今天 / 20 日均量 |
| | `up_vol_share20` | 20 日内上涨日成交量占比，量价配合，资金流入的代理 |
| | `obv_slope20` | 20 日净签名成交量 / 20 日总量，吸筹还是派发 |
| | `amt_ma20` | 20 日均成交额（对数），流动性水平 |
| 股东人数 | `gdhs_level` | 户数 / 流通股本，散户密度（原来四个都是变化量） |

### 实测（验证集 207 个交易日，逐月滚动，生产口径 ≥97 分且当天前 10 名，连续按上榜天数算）

三列对照。左：实验 8 发布的数字（带偷看）。中：只修偷看不加特征（消融，
`WF_DROP` 排除五个新特征）。右：修偷看 + 新特征（现在的生产模型）。
全市场基准改成验证集十个月的 2.93%（实验 8 用的 3.55% 混进了 2023~2024
训练月份，倍数被压低）。

| 连续上榜 | 实验 8（偷看） | 只修偷看 | 修偷看 + 新特征 | 样本（右） |
|---|---|---|---|---|
| ≥1 天（全部上榜） | 18.0% | 15.3% | **15.7%** | 918 |
| ≥2 天 | 25.7% | 15.7% | **18.9%** | 286 |
| ≥3 天 | 31.3% | 11.9% | **22.0%** | 150 |
| ≥4 天 | 32.6% | 7.9% | **25.0%** | 84 |
| ≥5 天 | 35.0% | 4.4% | **34.0%** | 47 |

三条结论：

1. **实验 8 的成绩有相当一部分是偷看给的。** 修掉之后整体从 18.0% 降到
   15.3%，连续 3 天从 31.3% 降到 11.9%。「连续够格」这个信号原来有一大半
   靠的是提前知道户数减少：户数一降（模型提前看到），那只票连着几天分数
   都高，而它之后确实涨了 —— 因为户数减少本身就是集中的证据，只是**现实中
   这时候还看不到**。
2. **新特征把连续信号救回来了，而且是单调的。** 只修偷看时连续 3 天以上
   反而比首日差（11.9% < 15.3%），加了成交量组和散户密度后恢复单调
   （15.7 -> 18.9 -> 22.0 -> 25.0 -> 34.0）。3 天档两边差 10 个百分点，
   各 150 个样本，约 2.3 个标准误；5 天档差 30 个百分点，约 4 个标准误。
3. **绝对水平比之前发布的低，这才是诚实的数字。** 邮件、面板、控制台的
   成绩条全部换成右列（export.STREAK_PERF），selftest 钉住常量和
   `window_grid.json` W5 行一致。

### 生产模型（refit，45 个特征，训练截止 2026-08-11）

新特征入选六个：`amt_ma20__last`（3.7%）、`amt_ma20__slope`（2.0%）、
`gdhs_level__slope`（4.0%）、`gdhs_level__last`（2.2%）、`up_vol_share20__last`
（0.6%）、`vol_ratio20__last`（0.3%）。`obv_slope20` 和 `up_vol_share20` 高度
相关，被相关剪枝丢了。

按组重要性：市场环境 41.2%（实验 8 是 27.3%）、结构 21.6%、位置 14.3%、
股东人数 9.9%（实验 8 是 17.0%，偷看贡献的那部分没了）、成交量 6.6%、
压缩 6.2%、交互项 0.1%。

**市场环境升到四成要记一笔**：偷看拿掉后，模型更依赖「今天是不是好日子」。
平盘市里清单的价值会更明显地下降，这一点比实验 8 时更严重。

### 分数分档（重算，仍严格单调）

| 分数段 | 样本 | 涨超 50% | 相对随便买 |
|---|---|---|---|
| 95 分以上 | 9,588 | 8.1% | 2.8× |
| 90~94 | 40,624 | 6.0% | 2.0× |
| 80~89 | 105,108 | 4.6% | 1.6× |
| 70~79 | 103,362 | 3.9% | 1.3× |
| 随便买 | 1,114,276 | 2.93% | 1.0× |

产物：`out_breakout/window_grid.json`（右列）、
`out_breakout/window_grid_exp9_ablation.json`（中列）、
`out_breakout/score_calibration.json`。

---

## 实验 10：把回测里剩下的两处漏洞也修掉（2026-09-15 全面审计）

实验 9 之后又查出两处让成绩偏乐观的地方，都不报错：

1. **走向前没做净化。** 预测 m 月时训练集取 `date < m-01`，紧挨 m 月前 20 个
   交易日的行，`y_up` 是用 m 月里的最高价算出来的，模型带着「m 月初谁会涨」
   进测试月。生产的 `load_or_fit` 用 `TRAIN_END_GAP=25` 截断是净化过的，
   回测没有。现在 `validate.purge_cut`：训练集截到测试月首日往前 20 个交易日，
   五个实验脚本统一走它。
2. **筹码网格用整段序列的 min/max。** 格宽由未来的最高价决定，一只票未来涨
   50% 创新高（正是标签为 1 的情形）时，它之前每一天的筹码特征都被扰动过。
   实测随机 40 只票，只用 [..t] 和用全序列算出的 chip_conc90 差最大 0.022，
   差值与「未来最高/过去最高」相关 0.79。现在按对数价格建网格，锚在第一根
   收盘价上覆盖 1/10~10 倍，360 格约 1.3%/格；selftest 钉住「截到 t 和用全序列
   算出的前 t 行逐位相同」。

### 实测（验证集 207 个交易日，生产口径 ≥97 分且前 10 名，连续按上榜天数，基准 2.93%）

| 连续上榜 | 实验 8（偷看） | 实验 9（修偷看+新特征） | **实验 10（再修净化+网格）** | 样本 |
|---|---|---|---|---|
| ≥1 天（全部上榜） | 18.0% | 15.7% | **12.6%**（4.3×） | 783 |
| ≥2 天 | 25.7% | 18.9% | **15.7%**（5.4×） | 216 |
| ≥3 天 | 31.3% | 22.0% | **14.3%** | 98 |
| ≥4 天 | 32.6% | 25.0% | **13.0%** | 54 |
| ≥5 天 | 35.0% | 34.0% | **6.7%** | 30 |

### 读法

1. **「连续够格是最强信号」这个结论撤回。** 它在实验 8 里有 31% 对 18% 的
   差距，实验 9 剩 22% 对 16%，三处漏洞都修掉后只剩 15.7% 对 12.6%（2 天档，
   216 只，差约 1.2 个标准误），3 天以上各档样本不到 100 只、数字上下抖动。
   清单仍按连续天数排（那是用户最初指令里的要求，排序无害），但邮件、面板、
   控制台的措辞全部改成「略高、样本少、不作数」。
2. **全部上榜 12.6% 对随便买 2.93%，4.3 倍，这是现在能给出的诚实数字。**
   分数分档仍严格单调（95 分以上 8.2%，99 分 13.3%，随便买 2.9%）。
3. 三轮下来成绩从 18.0% 一路降到 12.6%，每一步降的都是回测里偷来的部分，
   不是模型变差了。教训 30：每次改口径，把「回测/训练用的量」和「生产用的量」
   并排写出来对一遍。

### 生产模型（refit，45 个特征，训练截止 2026-08-11，指纹 482b519d）

按组重要性：市场环境 41.8%、结构 20.8%、位置 14.4%、股东人数 9.7%、
成交量 7.0%、压缩 6.3%。和实验 9 基本一样，筹码网格改法没有改变模型的结构。
model.json 现在记特征表指纹，列变了自动重训（以前要手点）。

产物：`out_breakout/window_grid.json`、`score_calibration.json`。

---

## 实验 11：实验 10 协议下的「有 / 无新特征」消融（2026-09-15 晚）

用户问：加了成交量和股东人数之后历史准确率为什么反而低了。实验 9 的消融是
在修偷看之后、修净化和筹码网格之前做的，这里在最终协议下重做一遍。两臂都在
**当天晚上重建的特征表**上算（09-15 数据追加、33 只票除权重拉、股东表刷新之后），
`WF_DROP` 排除 `vol_ratio20 / up_vol_share20 / obv_slope20 / amt_ma20 / gdhs_level`，
其余（20 日净化、对数筹码网格、逐月滚动、生产口径）完全一样。

### 实测（验证集 207 个交易日，基准 2.93%，± 是一个标准误）

生产口径（≥97 分且当天前 10 名，连续按上榜天数）：

| 连续上榜 | 有新特征 | 无新特征 | 差 |
|---|---|---|---|
| ≥1 天（全部上榜） | **13.6%** ±1.2（801） | 12.5% ±1.1（917） | +1.1 |
| ≥2 天 | **18.5%** ±2.6（222） | 14.5% ±2.1（269） | +4.0 |
| ≥3 天 | **21.1%** ±3.9（109） | 12.7% ±2.9（134） | +8.4 |
| ≥4 天 | **19.4%** ±5.0（62） | 7.0% ±3.0（71） | +12.4 |
| ≥5 天 | **14.3%** ±5.9（35） | 0.0%（42） | +14.3 |

≥97 分不限前 10（样本更大）：

| 连续够格 | 有新特征 | 无新特征 | 差 |
|---|---|---|---|
| ≥1 天 | 10.1% ±0.7（1640） | 9.3% ±0.7（1967） | +0.8 |
| ≥2 天 | 19.4% ±2.4（268） | 14.4% ±1.8（374） | +5.0 |
| ≥3 天 | 23.4% ±3.7（128） | 15.1% ±2.6（186） | +8.3 |
| ≥4 天 | 25.0% ±5.1（72） | 12.5% ±3.2（104） | +12.5 |
| ≥5 天 | 21.4% ±6.3（42） | 7.6% ±3.3（66） | +13.8 |

### 读法

1. **新特征不是准确率下降的原因，方向上是正贡献。** 全部上榜那一档两臂差
   1 个百分点，在噪声里；连续档位十个格子全部是「有 > 无」，差距随连续天数
   单调放大（单格 1~2 个标准误，十格同向就不是巧合）。没有这五个特征时，
   连续 4~5 天的票反而最差（7% / 0%）；有了它们连续档位才单调向上。
   和实验 9 的消融结论一致。
2. 新特征在走向前的 35 个入选特征里占 5 个：`amt_ma20__last/slope`、
   `gdhs_level__last`、`up_vol_share20__last`、`vol_ratio20__last`。

### 顺带发现：走向前成绩对特征表的日常变化很敏感

全特征这一臂和凌晨那份 `window_grid.json`（邮件里印的 STREAK_PERF 来源）
不是同一份表：中间隔了一天的数据维护。验证集的行和标签**逐位相同**
（基准 2.93114092% 到第 17 位一致，12075 个共同行的 y_up 无一不同），
特征筛选和拟合也是确定的（同一份表拟合两次，35 个特征、预测值逐位一样），
差别全部来自特征值的微小变化：

| 连续上榜 | 凌晨那份表 | 晚上重建的表 |
|---|---|---|
| ≥1 天 | 12.6%（783） | 13.6%（801） |
| ≥2 天 | 15.7%（216） | 18.5%（222） |
| ≥3 天 | 14.3%（98） | 21.1%（109） |

两次的清单 A 行只有 45% 重叠（352 / ~790），前 100 名的预测值相关只有 0.78。
全部上榜那档差 0.8 个标准误，连续档位差得多是因为样本小，这再次说明
连续 3 天以上的数字不能当信号用。**邮件里的常量不跟着每天的表漂**：
STREAK_PERF 仍取凌晨那份（更保守的一组），这里的数字只作对照。
生产侧的含义：模型每 30 天重训一次，重训后清单的成员会换掉一半左右，
这是「5000 只里取前 10」这种尖端选择的本性，不是故障。

产物：`out_breakout/window_grid_exp10_full.json`（有新特征）、
`window_grid_exp10_ablation.json`（无新特征）。`exp_window.py` 加了 `WF_OUT`，
消融不再覆盖生产那份 grid。

---

## 实验 12：全仓审计之后的口径重建（2026-09-16）

这一轮审计在起涨预测这条线上改了十处**口径**（不是加特征、不是调超参），
每一处都不报错，所以以前的成绩数字全部不能直接和重建后的比。
特征表和训练表整个重建、模型重训、成绩表重算，走 `tools/rebuild_all.py`。

### 重建后的成绩（2026-09-16 晚跑完）

特征表 4,283,710 -> 3,593,151 行，横截面不完整的日子 **96 -> 0 天**
（以前有 96 个交易日的当日全市场排名、板块×市值中性化、市场宽度是在
37~495 只票上算的），起点 2023-01-03 -> 2023-12-01（共同起点 + 120 根预热），
正样本率 3.42% -> 3.72%。模型 45 -> 51 个特征，指纹 482b519d -> 28d0fb53。

逐月滚动测试（验证集 207 个交易日，基准 2.93%）**第一次真的剔了 ST**
（`cache/st_codes.json`，`tools/dump_st.py` 抓的 204 只；以前回测不剔而生产剔，
是教训 30 那类口径不一致）。板块系数两个臂：

| 连续上榜 | 固定系数（生产在用） | 滚动估计（只用过去的月份） | 实验 10（旧表） |
|---|---|---|---|
| ≥1 天（全部上榜） | **13.5%** ±1.3（696） | 13.5% ±1.4（643） | 12.6%（783） |
| ≥2 天 | **15.0%** ±2.6（187） | 21.2% ±3.0（189） | 15.7%（216） |
| ≥3 天 | 9.8% ±3.3（82） | 22.9% ±4.3（96） | 14.3%（98） |
| ≥4 天 | 9.5%（42） | 20.7%（58） | 13.0%（54） |
| ≥5 天 | 4.5%（22） | 23.5%（34） | 6.7%（30） |

**邮件里印的是固定系数那一列**，因为生产此刻用的就是那套 `BOARD_ADJ`。
滚动臂不是「同一套策略的更诚实估法」，是**另一套策略**：两臂整体命中一样，
但板块构成完全不同 —— 固定臂主板 67% / 科创 30% / 北交 2.7%，滚动臂
主板 54% / **北交 31%** / 创业 12% / 科创 3.9%。它的连续档好看是因为反复上榜的
变成了北交所的票（连续≥3 天里北交占一半、命中 25%），而固定臂那一档里
科创占 23% 且**一只都没中**（n=19，0%）。这条要走会诊的提案流程去验证
（板块系数怎么估、要不要按滚动），不能直接拿来当成绩单。

### 两个必须写进邮件的高估来源

1. **满员日**。162 个非空日里只有 26 天是「够格的超过 10 只、被上限截断」，
   那 260 个名额命中 **7.69%**；其余 136 天（门槛在决定清单）的 436 个名额
   命中 **16.97%**，差一倍多。而生产 2026-09 那 8 天**全部满员**。
   够格的票一多，前 10 名里就混进大量刚过线的票。
   常量 `export.CAP_PERF` / `NONCAP_PERF`，邮件正文里明写。
2. **板块构成**。验证集里主板名额命中 16.1%（466）、科创 6.6%（211）、
   北交 26.3%（19，样本太少）。清单的构成每天不同，所以期望要按**本份清单**
   的构成加权：2026-09-16 那份是主板 6 / 科创 3 / 北交 1，加权期望 14.3%，
   而不是整体的 13.5%。数字从 `truth.validation_by_board()` 算，
   和 STREAK_PERF 同一份缓存，重训后自动跟着变。

### 连续天数这个信号，到此为止

实验 8 报过「连续 3 天 31.3% 对首日 18.0%」，实验 10 修掉三处偷看后降到
14.3% 对 12.6%，这一轮把口径也修干净之后，固定系数下是 **9.8% 对 13.5%
（反而更差）**。三次重测方向一次比一次弱，最后一次直接反号。
清单仍按连续天数排（那是用户最初的要求，排序无害），但邮件里不再给它
任何正面说法。

### 改了哪十处

| # | 改了什么 | 以前是什么 / 为什么错 | 在哪 |
|---|---|---|---|
| 1 | 筹码网格越界改**按同格宽动态外接** | 越界那天整天跳过：分布冻结、`out[i]` 不写，一只票创新高的那几天恰好是标签为 1 的情形 | `chips.py:44`（`CHIPS_VERSION=2`）、`chips.py:102-123` |
| 2 | 量能特征的基量从**成交股数**改成**换手率** | `daily.parquet` 的价是前复权、量未复权，送转后股数台阶式跳；每年约 290 次送转（集中在 5~7 月），10 送 10 之后 20 天里 `vol_ratio20` 从 2.0 衰减到 1.05，换手率恒定也能凭空造出一段「放量」 | `features.py:102-110` |
| 3 | 流通市值用**真实成交均价**还原复权因子 | `outstanding_share × 前复权收盘` 把除权日之前的市值按复权因子压小：`amount/(close×volume) > 1.2` 的行占 10.2%，300260 在 2023-06-15 算出 5.59e9 而真值 1.02e10。它是中性化的市值分档键，抽样四天 7.8% 的行分错档，而且偏差和标签同向（那批行 y_up 4.62% vs 3.28%）—— 复权因子由 t **之后**的分红送转决定，属教训 25 那类时间错位 | `build.py:126-146` |
| 4 | **共同起点 + 120 根预热**两道过滤 | 首次回填按根数截、除权重拉按日历截，2023-01-03~05-29 那 96 天只有 37~889 只票，当日全市场排名是在 37 只里排的，而且每次除权重拉都会再往这段薄片里塞一只（训练表在没有新信息的情况下天天自己漂）；预热行则是「几天成本冒充三年累积」，生产从不给这种票打分 | `build.py:51` `MIN_HIST`、`build.py:57` `COVER_MIN`、`trim_common_start`、`assemble` |
| 5 | 股东户数**过滤上市前名册** | 招股书/新三板阶段的名册只有几十到一千多户，上市后第一期的「上次」还是它，东财算出的增减比例中位 39220%、最大 1.63e8%（正常行最大约 460%）。当日 `gdhs_chg3` 前 1% 的行里 53.8% 是这种伪迹（y_up 10.49% vs 同区间非伪迹 4.21%），模型学到的是「股东特征极端 = 次新 = 容易起涨」，最近 8 份清单 A 里有 4 份混进这样的票 | `features.py:62` `HOLDER_MIN=1500`、`features.py:215-225` |
| 6 | 交互项改成**按假设指向相乘** | 原来写 `a * b`，而 a、b 都是中性化后以 0 为中心的量：(−)(+) 和 (+)(−) 同为负，假设象限和它的镜像象限拿到同一个值。428 万行实测 `x_light_top`：获利盘高&远前高 y_up 7.91% vs 获利盘低&近前高 2.31%（差 3.4 倍）被合并成同一个负值，pooled Spearman 从 −0.0217 被乘成 −0.0047 | `features.py:67-79`（`INTERACTION_VERSION=2`） |
| 7 | `mkt_breadth` **不把首行 NaN 当没涨** | `(s > 0).mean()` 里 `NaN > 0` 是 False：面板首日 2023-05-30 的 5039 行里 4151 行没有昨收，宽度被算成 0.098 而真值 0.559，凭空造出一个假暴跌日，还通过 5 日窗口污染随后 5 天 | `features.py:296-297` |
| 8 | **停牌占位行清洗** | 新浪给停牌日塞占位 K 线（价格照抄前一日、量和额为 0），已落盘的老分片里也有 | `backfill.py:133` `clean_bars`，落盘前和合并时各调一次 |
| 9 | 选票规则**统一走 `validate.pick`** | 生产走 `daily.stage_scan`（≥97 分、板块系数、截 10 只、风险剔除），验收走 `daily_topn`（每天固定前 10、什么都不带），两条路径没有任何共用代码：同一份逐月滚动打分，前 10 无门槛 11.16%（n=2070）vs 生产口径 12.64%（n=783），板块最强/最弱 1.63「通过」vs 6.20「重度不通过」 | `validate.py:102` |
| 10 | 板块系数加**逐月滚动估计的对照臂** | `BOARD_ADJ` 是拿验证集 2025-03..12 的各板块命中率除以整体命中率算的，而 `exp_window.py` 又在**同一段**数据上乘着它评 W5 —— 邮件里印的成绩含板块校正的样本内增益。反事实：带固定因子 n=783 命中 12.64%、连续≥2 天 15.74%；不带 n=588 命中 11.73%、连续≥2 天 11.83%（约 3.9 个百分点、相对高 33%，还是个下界） | `exp_window.py:58` `board_factors`，`--adj fixed/none/rolling`，产物里写 `adj_inconsistent` |

### 读法（现在就能说的）

1. 这十处**没有一处会报错**，全部是「算得出数、数是错的」。共同点和教训 30
   一样：回测/训练用的量和生产用的量不是同一个量。
2. 第 9、10 两处专门针对**验收本身**：以前的成绩不是在评价上线的那套规则。
   `--adj rolling` 那条臂加进来，就是为了以后任何一份成绩都能回答
   「这个数里有没有样本内增益」—— 产物自己写 `board_adj_mode` 和
   `adj_inconsistent`，不用靠人记得。
3. 成绩大概率还会再降一截（前三轮从 18.0% 一路降到 12.6%，每一步降的都是
   回测里偷来的部分）。降了不等于模型变差，等于数字终于说的是同一件事。
