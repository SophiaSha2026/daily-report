# 交接文档

最后更新：2026-08-23
接手方式：把整个目录交给 Claude Code，它会自动读 `CLAUDE.md`。

---

## 一句话状态

代码全部写完并通过离线测试，**尚未部署到 GitHub，也从未在真实交易日跑过**。
冒烟测试跑过一轮，暴露并修复了一个数据源问题。

---

## 现在在哪一步

| 阶段 | 状态 |
|---|---|
| 需求确认（15 个决策点） | 完成 |
| 代码实现（21 个文件） | 完成 |
| 离线自测 | 通过 |
| 冒烟测试（GitHub Actions） | 跑过一轮，发现东财批量接口不可用，已修复，**待重跑** |
| 建仓 + 上传 + 配 Secrets | **未做** |
| 真实交易日试跑 | **未做** |

---

## 下一步动作（按顺序）

1. 建仓 `SophiaSha2026/daily-report`，选 **Public**，网页上传全部文件
   （含 `.github` 目录，Windows 默认隐藏点开头文件夹，注意别漏）
2. 配 Secrets：`SMTP_HOST` `SMTP_PORT` `SMTP_USER` `SMTP_PASS` `MAIL_TO`
   `CLAUDE_CODE_OAUTH_TOKEN`（token 已拿到）。**不要填 `ANTHROPIC_API_KEY`**
3. 安装 Claude GitHub App：https://github.com/apps/claude ，只授权这一个仓库
4. Settings → Pages → Source 选 **GitHub Actions**（不是 Deploy from a branch）
5. Actions 标签页启用 workflows
6. 手动 Run `0-冒烟测试`，**关键项必须全绿**
7. 手动 Run `3-刷新板块缓存`（2-4 分钟，生成 `codes.csv` 和 `sector_map.parquet`）
8. 手动 Run `1-盘前候选池`，确认绿灯
9. **交易日 09:15 手动 Run `2-竞价选股`**，盯日志（见下面「必须盯的两处」）

详细网页操作步骤见 `README.md`。

---

## 冒烟测试结果（2026-08-23，GitHub runner）

```
[OK]   腾讯批量行情        1.0s   4/4 只，浦发昨收 9.11
[OK]   批量吞吐(1600只)   17.2s   ← 测试设计有误，见下
[FAIL] 东财全市场快照      40.6s   RemoteDisconnected
[OK]   交易日历            1.6s   8797 个交易日
[OK]   日线历史            0.9s   153 根 K 线
[OK]   打分逻辑自测        0.2s   12 边界 + 1000 压力 全通过
```

### 两个发现

**东财 `clist/get` 批量接口在 GitHub runner 上不可用。**
但同一轮里「日线历史」通过了，而 `stock_zh_a_hist` 走的也是东财
（`push2his.eastmoney.com`）。所以不是整站被封，只是这一个批量端点拒绝。

处理：全市场快照改走腾讯批量 + 本地代码表缓存。腾讯通道已验证可用。
盘前 08:23 调用时腾讯返回上一交易日收盘状态，正好是候选池需要的字段。
用户选了不限市值，唯一拿不到的总市值字段本来就用不上。

**「1600 只 → 1070 有效」是测试设计错误，不是限流。**
测试用了 `sh600000..sh600799` + `sz000001..sz000800` 这种编造连号，
其中一半不是上市代码。已改成从真实代码表取。

顺带把串行 17.2s 改成 5 路并发，mock 层实测 3.6s。
T3 在 09:25:10 抓，原来 17 秒后才结束，余量太薄。

---

## 真实交易日试跑时，必须盯的两处

**1. T1 快照那一行（09:19:40）**

如果返回的价格全等于昨收、涨幅全是 0，说明腾讯在竞价期间不吐虚拟撮合价。
那么 `假涨停撤单` 和 `竞价斜率` 两个维度是废的。

修法：改从东财盘前分时接口 `stock_zh_a_hist_pre_min_em` 取。
该接口返回 09:15–09:25 每分钟的撮合参考价（成交量字段在 09:25 前为 0）。
缺点是**单只**调用，1600 只票拉不动，需要先把 T1 的采集对象缩到评分靠前的
一小批，或者只对昨日涨停池做 T1。`datasource.py` 里留了位置。

**2. 各次快照的命中率**

日志形如 `T3: 1247/1580 只`。低于 80% 说明批量接口被限流，
把 `fetch_quotes` 的 `workers` 从 5 降到 3 再试。

---

## 已验证 vs 未验证

**已验证**（GitHub runner，2026-08）
- 腾讯批量行情可达，字段解析正确
- 东财单只日线接口可达
- 新浪交易日历可达
- 打分逻辑：12 个边界用例 + 1000 只压力样本
- `collect_llm.py` 三种失败场景均静默降级
- 通达信自定义数据格式 GBK/CRLF/4 字段校验
- 同花顺分层板块 txt 生成
- 全部 YAML + Python 语法，解压后复校验

**未验证**
- 腾讯在 09:15–09:25 是否返回虚拟撮合价（**最关键的一个**）
- claude-code-action 在纯 schedule 场景下是否必须装 Claude GitHub App
- 同时传 `claude_code_oauth_token` 和空的 `anthropic_api_key` 会不会报配置错误
  （如果 Claude 分析那步报认证类错误，**第一个**去 `auction.yml` 删掉
  `anthropic_api_key` 那一行）
- 端到端真实运行
- 同花顺剪贴板识别是否吃 GitHub Pages 复制过去的代码列表
- Gmail 从 GitHub runner 发信（冒烟测试的邮件步骤没跑到，被前面的失败中断了）

---

## 用户的 15 个决策

| # | 决策 |
|---|---|
| 1 | 仓库 `SophiaSha2026/daily-report` |
| 2 | Gmail SMTP（587 STARTTLS，应用专用密码） |
| 3-4 | 用同花顺，不用通达信 |
| 5 | 接受 `AUC_RATIO` 口径，但要求附理由 + LLM 辅助分析 |
| 6 | A/B 两组**合并**成一张榜 |
| 7 | 合并后取**前 10** |
| 8 | **只发一次**，稍晚时段发（当前 09:28:30） |
| 9 | 北交所**不排除** |
| 10 | 科创板保留 |
| 11 | **不限市值** |
| 12 | Opus 5；撞额度就发邮件声明 |
| 13 | 自定义数据用默认编号 |
| 14 | 空榜也发信 |
| 15 | 不需要交易建议，只要清单 |

后续追加：走 OAuth 路线（token 已生成）。

---

## 过程中纠正的判断

按发生顺序，都写进 `CLAUDE.md` 的「历史教训」了：

1. 用户原始规则里「竞价量能 ≥ 昨日全天 10%」与「量比 2.5–10」自相矛盾，
   交集接近空集。换成 `AUC_RATIO` 1.5%–8%。
2. 「高开 2%–5%」对 10/20/30cm 板块含义完全不同，改用 `GAP_NORM` 归一化。
3. 我最初判断 OAuth 在非交互环境走不通——**错了**。`claude setup-token`
   生成的长期 token 正是为 CI 设计的，但只对 Claude Code 有效，
   会被 Messages API 拒绝，所以必须走 claude-code-action。
4. 我把通达信的「扩展数据」和「自定义数据」搞混了。前者只能由本地公式生成，
   不能外部导入；能导入的是后者。
5. 同花顺**根本没有**外部数据导入这个功能。路线 C 在同花顺上降级为分层板块 +
   HTML 面板。
6. `prompt_file` 不是 claude-code-action v1 的合法参数，会报 unknown input。
   改用内联 `prompt` + `--json-schema` 结构化输出。
7. 冒烟测试的 1600 只吞吐测试用了编造代码，造成限流误判。
8. `hard_reject` 里假涨停和尾盘跳水的判定顺序反了，会给出错误的拒绝原因。

---

## 待办与开放问题

**待办**
- [ ] 部署（上面 9 步）
- [ ] 交易日试跑，验证 T1 虚拟撮合价
- [ ] 攒够 40 个交易日数据后写 `calibrate.py`，用自己的历史数据标定
      `config.yaml` 里所有阈值。目前所有阈值都是先验估计，没有实证支撑。

**开放问题（用户没回答过）**
- 「稍晚时间段再复查」当前实现为 T4 补采 + 09:28:30 发信。用户是否还想要一封
  开盘后（如 10:00）或收盘后的表现回填邮件？如果要，需要新写 `postmarket.py`
  和对应 workflow。
- 是否需要 Server酱 / 企业微信机器人做低延迟推送（邮件之外的通道）。
- LLM 撞额度如果成为常态，是否接受把 `--model` 换成 `claude-sonnet-5`。
  这活儿是查证 + 写 28 字短句，Sonnet 够用，额度消耗低一档。

**已知会消耗订阅额度**
OAuth token 走的是用户订阅的同一个池子，不是独立配额。用户日常用得多的话，
第二天早上的分析步骤可能因额度不足失败。失败不阻断，但会收到没有理由文案的清单。

---

## 时间线（北京时间）

```
08:23     构建候选池（全市场 → 400-1600 只，扫隔夜公告）
08:47     竞价任务启动（cron 有 0-30 分钟抖动，故提前 38 分钟）
08:59     第二入口
09:11     第三入口（兜底）
09:14     预热连接
09:19:40  T1  撤单前虚拟撮合价
09:23:30  T2
09:25:10  T3  最终竞价结果
09:26:30  硬死线，超过放弃并发告警
09:27:20  T4  补采 T3 漏掉的票（价格此时已固定不变）
09:27:40  Claude 分析（硬超时 150 秒）
09:28:30  发邮件 + 更新 Pages 面板
周日20:17 刷新行业成分 + 代码表缓存
```

用户在美东时区：北京 09:28 = 美东 21:28（夏令时）/ 20:28（冬令时）。

---

## GitHub Actions 的两个坑

**cron 是尽力而为**，不是定时器。官方承认高负载会延迟，社区实测整点附近
常见 5–30 分钟，极端时整个被丢弃。所以设计成：提前 38 分钟触发 +
三个错峰入口 + job 内自旋等待到精确时刻 + 幂等检查防重复发信。

**仓库 60 天无提交，cron 会被自动停用。** 流水线每天 commit 当日快照数据到
`data/`，天然充当 keepalive。不要为了"减少仓库体积"把这个 commit 去掉。
