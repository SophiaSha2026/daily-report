# 交接文档

最后更新：2026-08-24（北京时间）
接手方式：把整个目录交给 Claude Code，它会自动读 `CLAUDE.md`。

---

## 一句话状态

已部署上线，冒烟测试全绿，盘前候选池与竞价流水线均已在 GitHub Actions 上
真实跑通。2026-08-24 做了首次交易日实盘试跑。

---

## 现在在哪一步

| 阶段 | 状态 |
|---|---|
| 需求确认（15 个决策点） | 完成 |
| 代码实现 | 完成 |
| 离线自测 `selftest.py` | 通过 |
| 建仓 + 上传 + 配 Secrets | 完成（6 个 secret 都在） |
| Pages 配置 | 完成（Source = GitHub Actions，已验证 `build_type: workflow`） |
| 冒烟测试 | **全绿**（2026-08-24，7/7 关键项） |
| 盘前候选池 | **跑通**（1092 只，已提交入库） |
| 板块成分表 | **完成且全自动**，同花顺 90 个行业 / 5547 只，全市场覆盖 5546/5548 |
| 交易日实盘试跑 | **2026-08-24 首次，全绿**，见下节 |
| 全自动化 | 完成，日常零人工，运维见 `OPERATIONS.md` |

---

## 2026-08-24 这轮改了什么

上一版交接说「冒烟测试跑过一轮，已修复，待重跑」。重跑后又红了两项，
根因是**东财整站在 GitHub runner 上不可达**（上一轮只是批量接口不通）。
按此改了五处：

**1. 代码表多源 + 重试**（`datasource.refresh_code_list`）
东财两个代码表接口间歇性 `ConnectionReset`。加了新浪 hs_a 分页源，
每个源重试 2 次。另外把 `cache/codes.csv`（5548 只）直接提交进仓库，
日常根本不需要现拉。

**2. 日线东财优先 + 腾讯兜底 + 熔断**（`datasource.daily_hist`）
东财 `stock_zh_a_hist` 仍是首选（成交额是真实值），单只重试 3 次。
但盘前要对 1600 只逐个拉，东财整段不通时每只耗满重试会撑爆 40 分钟超时，
所以加熔断：连续 12 只失败就整体切腾讯 K 线，每 150 只回探一次东财。
实测当天东财 0 只成功、腾讯 1031 只成功，熔断按预期生效。

**3. 盘前日线改并发**（`premarket.stage2`）
1092 只串行 9 分钟以上，4 路并发压到 83 秒。

**4. 候选池必须提交入库**（`premarket.yml`）
原来只 `upload-artifact`，但竞价是另一个 job、另一次 checkout，
读不到 `cache/universe.parquet`，每天都会走「候选池缺失」告警分支。
**这个 bug 会让流水线每天空转，之前没被发现是因为从没端到端跑过。**

**5. 竞价 workflow 加串行组**（`auction.yml`）
幂等检查看的是 `data/auction_<date>.parquet`，而它 09:27 才落盘。
08:47/08:59/09:11 三个 cron 入口 checkout 时全都看不到，会并行跑完
各发一封邮件。加 `concurrency: auction-daily` 让后来的排队，
等前一个提交完数据再 checkout，幂等检查才真正生效。

**6. 「未分类」不参与板块共振**（`run_auction.build_features`）
板块缓存缺失时 sector 全填「未分类」，`f_sector` 会把它当成一个几百只
成分的巨型板块，给每只都打满分，白送 15 分权重。计数时剔掉这个占位符。

---

## 2026-08-24 实盘试跑结果

全链路跑通，邮件已发。关键数字：

```
候选池 1092 只
T1: 1092/1092 只, 1.6s   ← 09:19:40，腾讯在竞价期间**确实**吐虚拟撮合价
T2: 1092/1092 只, 1.5s   ← 09:23:30
T3: 1092/1092 只, 1.3s   ← 09:25:10
T4: 1092/1092 只, 1.6s   ← 09:27:20
特征 1092 只，初筛通过 29 只 -> 入选 10 只
Claude 分析: structured_output 10 条，全部是具体事实
清单邮件已发送 (清单, 10 只, 4 附件)
```

**四次快照命中率都是 100%**，单次 1.3-1.6 秒，离 09:26:30 的硬死线有大量余量。
5 路并发没有触发腾讯限流。

**上一版交接里那个「最关键的未验证项」已验证**：腾讯在 09:15-09:25 期间返回
虚拟撮合价，T1 的涨幅不是 0，斜率维度是活的（当天首位 600613 斜率 +8.56）。

**Claude 分析这一步不需要额外配置**：OAuth token + claude-code-action 直接跑通，
不需要装 Claude GitHub App，`anthropic_api_key` 留空也没报配置错。

当天唯一的缺憾：板块表是在竞价跑起来之后才做好的，所以这一轮 sector 全是
「未分类」，板块共振维度对所有票都是 0 分。次日起正常。

跑完发现并修掉的问题见下节。

---

## 板块成分表：为什么必须开真浏览器

试过的源，逐个说明为什么不行：

| 源 | 结果 |
|---|---|
| 东财 `stock_board_industry_*_em` | GitHub runner 和本机都 `RemoteDisconnected` |
| 新浪 `newSinaHy` + `Market_Center` | 那张表**本身过期**：只认 3000 只，688/300/301/920 整段缺（缺 2924 只）；runner 上该主机还直接返回空体 |
| 同花顺 akshare 封装 | **akshare 1.18 已经删掉 `stock_board_industry_cons_ths`**，只剩 name/index/info/summary |
| 同花顺自己带 cookie 请求 | 用 node 跑 akshare 自带的 ths.js 算出 v 值（py_mini_racer 新版 C API 不兼容，调不动），requests 带上仍然 **403 Nginx forbidden** |
| 同花顺真浏览器 fetch | **可用**。同一台机器、同一个出口 IP，浏览器会话里 fetch 同一个 URL 直接 200。GitHub runner 上用 Playwright 起 chromium 同样可用 |
| 深交所官方 xlsx | 能下，但行业字段是证监会门类（"J 金融业"，19 类），粒度太粗 |
| 申万 `sw_index_third_cons` | 能用但限速极重，335 个三级行业跑了 10 分钟只拿到 131 条 |

结论：**挡的是客户端指纹，不是 IP**。所以 `src/refresh_sector.py` 用 Playwright
起 chromium，在页面上下文里发 fetch。

另一个坑：成分接口在**第 5 页（100 只）硬截断**，但 `page_info` 会报真实页数
（半导体报 1/10，实际第 6 页起返回空体）。解法是同一排序字段 desc + asc 各取
5 页合并，覆盖 200 只以内的行业；超过 200 只的行业再换现价/成交额/流通市值/
市盈率四个字段补中段（field id 在表头 `<a field=...>` 里）。

结果：90 个行业 / 5547 只，全市场 5548 只里**覆盖 5546 只**，runner 上约 7 分钟。
（本地浏览器手抓那版只有 5422 只，差在大行业没补中段。）

## 真实交易日必须盯的两处

**1. T1 快照那一行（09:19:40）** —— 2026-08-24 已验证正常，但值得长期留意

如果哪天返回的价格全等于昨收、涨幅全是 0，说明腾讯不再吐虚拟撮合价，
那么「假涨停撤单」和「竞价斜率」两个维度就废了。

修法：改从东财盘前分时接口 `stock_zh_a_hist_pre_min_em` 取
（返回 09:15-09:25 每分钟撮合参考价）。但它是**单只**调用，1600 只拉不动，
需要先把 T1 采集对象缩到评分靠前的一小批。而且东财现在整站不通，
真要走这条路得先解决可达性。

**2. 各次快照的命中率**

日志形如 `T3: 1247/1580 只`。低于 80% 说明批量接口被限流，
把 `fetch_quotes` 的 `workers` 从 5 降到 3 再试。

---

## 已验证 vs 未验证

**已验证**（GitHub runner，2026-08-24）
- 腾讯批量行情：5548 只全返回，1600 只并发 1.7s
- 腾讯日 K：153 根，字段与东财对齐
- 新浪交易日历：8797 个交易日
- 打分逻辑：12 边界用例 + 1000 压力样本
- 盘前两阶段收缩：5548 → 1092，全程 3m58s
- 候选池提交入库 → 竞价 job checkout 能读到
- Pages 配置为 GitHub Actions 构建

- 腾讯 09:15-09:25 返回虚拟撮合价（T1 斜率非 0，当天首位 +8.56）
- claude-code-action + OAuth token 直接跑通，**不需要装 Claude GitHub App**
- Gmail 从 runner 发信成功（10 只 4 附件）；Pages 部署成功
- 带 token 的 git push 成功（裸 push 在 claude-code-action 之后必失败）
- 板块表挂进候选池：未分类占比从 100% 降到 0.6%

**未验证 / 已知不可用**
- 东财全部接口时通时不通（08-24 盘前那轮 107 只成功、516 只走腾讯兜底）
- 新浪 vip 主机在 runner 上返回空体
- 同花顺剪贴板识别是否吃 Pages 复制过去的代码列表
- 隔夜公告过滤（`stock_notice_report` 走东财，当天 IncompleteRead）

---

## 用户的 15 个决策

| # | 决策 |
|---|---|
| 1 | 仓库 `SophiaSha2026/daily-report` |
| 2 | Gmail SMTP（587 STARTTLS，应用专用密码） |
| 3-4 | 用同花顺，不用通达信 |
| 5 | 接受 `AUC_RATIO` 口径，但要求附理由 + LLM 辅助分析 |
| 6 | A/B 两组**合并**成一张榜 |
| 7 | 合并后取**前 10** |
| 8 | **只发一次**，稍晚时段发（当前 09:28:30） |
| 9 | 北交所**不排除** |
| 10 | 科创板保留 |
| 11 | **不限市值** |
| 12 | Opus 5；撞额度就发邮件声明 |
| 13 | 自定义数据用默认编号 |
| 14 | 空榜也发信 |
| 15 | 不需要交易建议，只要清单 |

后续追加：走 OAuth 路线（token 已生成并配好）。

---

## 待办与开放问题

**待办**
- [ ] 观察 cron 是否稳定自动触发（目前唯一还没被验证的自动化环节）
- [ ] 攒够 40 个交易日数据后写 `calibrate.py`，用自己的历史数据标定
      `config.yaml` 里所有阈值。目前所有阈值都是先验估计，没有实证支撑
- [ ] 观察几个交易日，确认 cron 是否稳定触发（08-24 当天三个 cron 都没自动
      触发，是手动 dispatch 跑的；新仓库的 schedule 生效有延迟）

**开放问题（用户没回答过）**
- 「稍晚时间段再复查」当前实现为 T4 补采 + 09:28:30 发信。是否还想要一封
  开盘后（如 10:00）或收盘后的表现回填邮件？要的话需新写 `postmarket.py`
- 是否需要 Server酱 / 企业微信机器人做低延迟推送
- LLM 撞额度如果成为常态，是否接受把 `--model` 换成 `claude-sonnet-5`

**已知会消耗订阅额度**
OAuth token 走的是用户订阅的同一个池子，不是独立配额。

---

## 时间线（北京时间）

```
08:23     构建候选池（全市场 → 1000-1600 只，扫隔夜公告）
08:35     竞价第一入口（cron 有 0-30 分钟抖动，故提前触发）
08:47     第二入口
08:59     第三入口
09:05     第四入口
09:11     第五入口（最后一班）
09:14     预热连接
09:19:40  T1  撤单前虚拟撮合价
09:23:30  T2
09:25:10  T3  最终竞价结果
09:26:30  硬死线，超过放弃并发告警
09:27:20  T4  补采 T3 漏掉的票（价格此时已固定不变）
09:27:40  Claude 分析（硬超时 150 秒）
09:28:00  发邮件 + 更新 Pages 面板
周日20:17 刷新代码表
周日20:40 刷新同花顺板块成分表（Playwright 真浏览器）
```

用户在美东时区：北京 09:28 = 美东 21:28（夏令时）/ 20:28（冬令时）。

日常运维见 `OPERATIONS.md`：正常情况下零人工。

---

## GitHub Actions 的坑

**cron 是尽力而为**，不是定时器。08-24 当天 08:23 的盘前 cron 和三个竞价
cron **一个都没触发**（新仓库 schedule 生效有延迟），全靠手动 dispatch。
设计上已有三个错峰入口 + job 内自旋等待 + 幂等检查，但要留意头几天。

**仓库 60 天无提交，cron 会被自动停用。** 流水线每天 commit 当日快照数据到
`data/`，天然充当 keepalive。不要为了"减少仓库体积"把这个 commit 去掉。

**跨 job 传状态只能靠提交或 download-artifact。** 每个 job 是独立 runner、
独立 checkout。盘前的 `upload-artifact` 竞价 job 看不见——这个坑已经踩过。
