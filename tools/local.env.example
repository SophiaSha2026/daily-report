# 本地完整跑时用来发信的 SMTP 配置。复制成 local.env 再填。
# local.env 已在 .gitignore 里排除，不会进仓库。
# 这几个值和 GitHub Secrets 里那套一模一样。
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=你的Gmail地址
SMTP_PASS=Gmail应用专用密码（16位，不是登录密码）
MAIL_TO=收件地址
GH_OWNER=SophiaSha2026
GH_REPO=daily-report
