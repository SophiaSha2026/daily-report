回答一个问题：**数据和流程有没有问题在制造假差距。**

盯这些：
1. `breakout.data_health`：每天参与横截面的代码数是否稳定（薄日 thin_days）、最早那些日期
   只有几十只票；最新清单有没有特征 NaN；日期范围是否到最近交易日
2. `breakout.lists[]`：bars 已走几根是否和日历一致（比如 09-07 到 09-16 之间该有几个交易日）；
   `n` 是否总是 10（说明 ≥97 分的门槛从没卡住，和验证集里平均 3.9 只/天不一样——
   这本身就是「口径不一致」）
3. `morning.daily[]`：pool 大小是否异常、admitted 比例是否稳定；有没有整天被丢的（抢救日）；
   `morning.backfill_vs_online.rows` 里哪几列差得大（回填 t1/t3 是代理值；one_word、
   limit_pct 定义不同；候选池过滤不同）
4. 时间对齐：早盘标签是「开盘买收盘卖」，开盘价用的是竞价成交价（open_mismatch 超过 0.5% 算脏）；
   起涨预测的真值窗口是 20 根自身 K 线，停牌的票会拉长；同期基准是不是同一天同一窗口
5. 单位：成交额 元/万元、换手率 %/小数、股本 股/万股；证据包里的数字量级对不对
6. 已知会引起偏差的流程：起涨预测的风险剔除（ST/减持/解禁/增发）在验证集里没做、生产做了；
   清单 A 的连续天数从重算日起算；A 池 60 交易日过期按自然日近似

用查询工具核对具体数字（`lists`、`picks --date`、`stock --code`、`morning-day --date`）。

输出：findings 写「什么数据/流程问题，影响哪个数字，大概偏多少」；proposals 用 data_fix / process，
每条写清怎么验证修好了。你不能改代码，只能指出。
